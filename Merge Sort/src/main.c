#include "../headers/main.h"

/*
    Author: Sebastiano D'Angelo
*/

int main(){
    
    
    return 0;
}

void merge(int arr[], int l, int m, int r) {
    int i, j, k;
    int aux[l];
    for(i = m + 1; i > l, i--) arr[i - 1] = arr[]
}